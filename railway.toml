[build]
builder = "NIXPACKS"
# buildEnvironment = "V2"
nixpacksConfigPath = "railway.build.toml"

[deploy]
# region = "us-west1"
runtime = "V2"
numReplicas = 1
# healthcheckPath = "/health"
# healthcheckTimeout = 90
# sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
